# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Kiosk runtime data (created by dashboard_app.py next to the script)
downwell_sessions/
downwell_saved_games/
usage_totals.json

# per-run high scores inside game templates
game_templates/*/high_scores.txt

# RetroPie push target (host + credentials) - local only
retropie.json

# kiosk process logs (redirected stdout/stderr)
kiosk_stdout.log
kiosk_stderr.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
